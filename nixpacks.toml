[phases.setup]
nixPkgs = ["gettext"]

[phases.build]
cmds = [
  "python manage.py collectstatic --noinput",
  "python manage.py compilemessages"
]

[start]
cmd = "gunicorn portfolio_project.wsgi --log-file -"
